# Seanime (large local repository directory)
seanime/
/seanime

# Dependencies & Environment
node_modules/
.env
.env.*
!.env.example

# Output / Build logs
dist/
build/
logs/
.DS_Store

# Local tooling and the owner's original art drop (processed copies live in site/assets)
.claude/
art-source/
