export const createDownload = () => {
  const error = new Error("Download engine has not been implemented.");
  error.statusCode = 501;
  throw error;
};

export const getDownloadStatus = (id) => {
  const error = new Error("Download engine has not been implemented.");
  error.statusCode = 501;
  throw error;
};

export const cancelDownload = (id) => {
  const error = new Error("Download engine has not been implemented.");
  error.statusCode = 501;
  throw error;
};
